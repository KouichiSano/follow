<% if book.favorited_by?(current_user) %>
  <%= link_to "いいね解除", book_favorites_path(book.id), method: :delete, remote: true %>
<% else %>
  <%= link_to "いいね",  book_favorites_path(book.id), method: :post, remote: true %>
<% end %>